#ifndef ARGS_H_INCLUDED
#define ARGS_H_INCLUDED
#include <stdio.h>

/* Parses the system's arguments and return the percentage to fill memory */
size_t parse_args(int _argc, char** _argv);

#endif /* ARGS_H_INCLUDED */
